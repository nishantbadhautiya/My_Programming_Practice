#include <stdio.h>

int main()
{
    // int num;
    // scanf("%d", &num);
    // while (num > 10)
    // {
    //     printf("%d \n", num);
    //     num++;
    // }
    float num = 0;
    while (num <= 20)
    {
        if (num >= 10)
        {
            printf("%f\n", num);
        }
        num++;
    }

    return 0;
}