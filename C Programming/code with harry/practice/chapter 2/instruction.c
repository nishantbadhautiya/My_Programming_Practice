#include <stdio.h>

int main()
{
    // int a, b, c;
    // a = 3;
    // b = c = a;
    // printf("the value of a is %d \n", a);
    // printf("the value of b is %d \n", b);
    float a = 3.5;
    float b = a + 3.6;
    printf("the value of b is %f \n", b);
    return 0;
}